# Applications

Here we will place ArgoCD application manifests. ArgoCD is watching this folder for any changes, meaning
if you (or xctl) place an application manifest here, ArgoCD will automatically pick it up and deploy it.