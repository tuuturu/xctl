# Namespaces

Here we will place namespace manifests. ArgoCD is watching this folder for any changes, meaning
if you (or xctl) place a namespace manifest here, ArgoCD will automatically pick it up and deploy it.
